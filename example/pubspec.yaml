name: example
description: Demonstrates how to use the logging_flutter plugin.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.12.0 <4.0.0"
  flutter: ">=3.47.0"

dependencies:
  flutter:
    sdk: flutter

  logging_flutter:
    path: ../

  logging: "^1.3.0"

flutter:
  uses-material-design: true
